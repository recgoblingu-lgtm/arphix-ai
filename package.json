{
  "name": "arphix-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first AI coding and voice assistant maker",
  "type": "module",
  "scripts": {
    "start": "node local-runtime/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "node --check web/app.js && node --check web/engine.js && node --check local-runtime/server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
